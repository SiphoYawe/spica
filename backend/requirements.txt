# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# SpoonOS - Install from local source
# Note: In Docker, spoon-core is mounted at /spoon-core
# For local dev, install with: pip install -e ../spoon-core
# SpoonOS dependencies (from spoon-core/pyproject.toml)
openai>=1.70.0
sqlalchemy>=2.0.38
aiohttp>=3.13.2

# Neo N3
neo3-boa==1.4.1  # Latest version compatible with Python 3.13
neo-mamba==3.1.0  # Latest version - 2.x series doesn't support Python 3.13

# HTTP Client (httpx only - more modern and async-first)
httpx==0.27.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.9
base58==2.1.1

# Testing
pytest==8.3.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
