# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# SpoonOS - Install from local source
# Note: In Docker, spoon-core is mounted at /spoon-core
# For local dev, install with: pip install -e ../spoon-core
# SpoonOS dependencies (from spoon-core/pyproject.toml)
openai>=1.70.0
sqlalchemy>=2.0.38
aiohttp==3.10.5  # Pinned to match neo-mamba requirement

# Neo N3
neo3-boa==1.3.0  # Compatible with Python 3.11
neo-mamba==2.7.0  # Latest 2.x version compatible with Python 3.11

# HTTP Client (httpx only - more modern and async-first)
httpx==0.27.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.9
base58==2.1.1
filelock==3.16.1  # Cross-platform file locking for workflow storage

# Testing
pytest==8.3.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
