# Spica Backend Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# LLM Configuration (Required)
# ============================================================================
# OpenAI API key for SpoonOS AI agents
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================================================
# Neo N3 Configuration (Required)
# ============================================================================
# Neo N3 Testnet RPC endpoints
NEO_TESTNET_RPC=https://testnet1.neo.coz.io:443
NEO_TESTNET_RPC_FALLBACK=https://testnet2.neo.coz.io:443
NEO_RPC_TIMEOUT=60

# Demo Wallet WIF (CRITICAL - Read instructions below)
# This wallet is used for executing workflow transactions on Neo testnet
#
# HOW TO CREATE A DEMO WALLET:
# 1. Go to Neo N3 testnet faucet: https://neoxwish.ngd.network/
# 2. Click "Create Wallet" or use Neo CLI: neo-cli create wallet
# 3. Save the WIF (starts with 'K' or 'L', 52 characters)
# 4. Fund the wallet with 1000+ GAS from the faucet
# 5. Paste the WIF below
#
# SECURITY NOTES:
# - This is for TESTNET ONLY - never use mainnet keys
# - Never commit the actual .env file to version control
# - The WIF is never logged or exposed in API responses
# - Only the public address and balances are exposed
#
# Example WIF format: L1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKL
DEMO_WALLET_WIF=your-neo-wallet-wif-here

# ============================================================================
# x402 Payment Configuration (Required)
# ============================================================================
# x402 facilitator service URL
X402_FACILITATOR_URL=https://x402.org/facilitator

# Ethereum address to receive x402 payments (Base Sepolia)
# This should be YOUR address for receiving USDC payments
# Format: 0x followed by 40 hex characters
X402_RECEIVER_ADDRESS=0x89774730A599caaBe6B75aC7d2DC9B8dADE0F817

# x402 network (testnet for demo)
X402_NETWORK=base-sepolia

# Default payment asset
X402_DEFAULT_ASSET=USDC

# ============================================================================
# CoinGecko API Configuration (Optional)
# ============================================================================
# CoinGecko API URL (no key required for basic usage)
COINGECKO_API_URL=https://api.coingecko.com/api/v3

# ============================================================================
# Application Configuration (Optional)
# ============================================================================
# Application environment: development, production
ENVIRONMENT=development

# Debug mode
DEBUG=true

# Application name
APP_NAME=Spica

# Workflows storage directory (relative to backend/)
WORKFLOWS_DIR=./workflows

# ============================================================================
# SETUP CHECKLIST
# ============================================================================
# [ ] Get OpenAI API key from platform.openai.com
# [ ] Create Neo testnet wallet at neoxwish.ngd.network
# [ ] Fund wallet with 1000+ GAS from faucet
# [ ] Save WIF to DEMO_WALLET_WIF
# [ ] Set your Ethereum address for X402_RECEIVER_ADDRESS
# [ ] Copy this file to .env
# [ ] Never commit .env to git (it's in .gitignore)
